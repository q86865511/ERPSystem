# Structured (JSON) console logging — activated only under the 'json' profile (the observability overlay
# sets SPRING_PROFILES_ACTIVE=seed,json). Dev, tests and the plain demo stay human-readable. Spring Boot's
# built-in ECS structured format includes MDC, so the request correlationId rides along as a field.
logging.structured.format.console=ecs
